# Portability Position

MIGRATE-OS prefers conventional, exportable technologies and keeps business rules outside vendor-specific interfaces. GitHub stores code and migrations; PostgreSQL stores operational data; hosting remains replaceable; external services are isolated behind adapters; source identifiers and evidence remain traceable.

A target platform limitation must be recorded and explicitly accepted rather than disguised as portability.
