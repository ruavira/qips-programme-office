# Next Objective

Install MIGRATE-OS against an independent export of the ITC Network Operations System and execute Phases 1–3 without altering the live Base44 application:

1. verify source checkpoint and independent restore package;
2. export entity data and files with checksums and record counts;
3. run deterministic source discovery;
4. create entity/field and workflow/action mappings;
5. define the target PostgreSQL, Auth, Storage and RLS architecture;
6. establish a non-production Supabase target;
7. select the first vertical slice for rehearsal.

No production activation, external users, real-data migration, DNS change or source decommission is authorized by this registration.
